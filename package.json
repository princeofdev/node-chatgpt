{
  "name": "chatgpt",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "complete": "node -r dotenv/config complete.js",
    "fine-tune": "node -r dotenv/config fine-tune.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cli": "^1.0.1",
    "dotenv": "^16.0.3",
    "openai": "^3.1.0"
  }
}
